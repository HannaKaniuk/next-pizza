DATABASE_URL="postgresql://user:password@localhost:5432/next_pizza"

# Auth.js — згенеруй: openssl rand -base64 32
AUTH_SECRET="change-me-in-production"
AUTH_URL="http://localhost:3000"

# Email (Resend) — у dev без ключа код у консолі сервера
RESEND_API_KEY=""
EMAIL_FROM="Next Pizza <onboarding@resend.dev>"
